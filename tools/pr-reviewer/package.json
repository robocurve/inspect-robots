{
  "name": "inspect-robots-pr-reviewer",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "types": "mkdir -p generated && wrangler types --config wrangler.jsonc --config publisher.wrangler.jsonc --config runner.wrangler.jsonc --env-interface ReviewerEnv --strict-vars=false --include-runtime=false generated/reviewer.d.ts && wrangler types --config publisher.wrangler.jsonc --env-interface PublisherEnv --include-runtime=false generated/publisher.d.ts && wrangler types --config runner.wrangler.jsonc --config wrangler.jsonc --env-interface RunnerEnv --include-runtime=false generated/runner.d.ts",
    "check": "npm run typecheck && npm test",
    "deploy:publisher": "wrangler deploy --config publisher.wrangler.jsonc",
    "deploy:reviewer": "wrangler deploy --config wrangler.jsonc",
    "deploy:runner": "wrangler deploy --config runner.wrangler.jsonc"
  },
  "dependencies": {
    "@cloudflare/sandbox": "^0.12.9",
    "jose": "^6.2.12",
    "openai": "^7.20.0",
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "@cloudflare/vitest-plugin": "^1.1.13",
    "@cloudflare/workers-types": "^5.20260920.1",
    "@types/node": "^26.6.2",
    "typescript": "^7.0.2",
    "vitest": "^4.1.11",
    "wrangler": "^4.135.0"
  }
}
